export interface AppVolume监控ingUsageModel {
    projectId: string,
    project名称: string,
    app名称: string,
    appId: string,
    mountPath: string,
    usedBytes: number,
    capacityBytes: number,
    isBaseVolume: boolean
}
